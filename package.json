{
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development nodemon",
    "staging": "NODE_ENV=staging node server.js",
    "prod": "NODE_ENV=production node server.js",
    "deploy:staging": "cp .env.staging .env && npm run staging",
    "deploy:prod": "cp .env.production .env && npm run prod"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "winston": "^3.19.0"
  }
}
